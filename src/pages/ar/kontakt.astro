---
import Base from '../../layouts/Base.astro';
import Sections from '../../components/sections/KontaktSections.astro';
import { pageT } from '../../i18n/pages';

const t = pageT('kontakt', 'ar');
---
<Base title={t.metaTitle} description={'metaDesc' in t ? t.metaDesc : undefined}>
  <Sections />
</Base>
