---
import Base from '../../layouts/Base.astro';
import Sections from '../../components/sections/AnmeldungSections.astro';
import { pageT } from '../../i18n/pages';

const t = pageT('anmeldung', 'en');
---
<Base title={t.metaTitle} description={'metaDesc' in t ? t.metaDesc : undefined}>
  <Sections />
</Base>
